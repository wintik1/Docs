# scxcaptchaAjaxForm

Неблокирующая числовая CAPTCHA (5 цифр) для **AjaxForm** и **FormIt**.  
Простая для человека, сложнее для ботов. Все имена и классы имеют префикс `scx_`.

---

## 🆕 Что нового в версии 1.0.1

- Исправлена проблема с обновлением капчи после отправки формы.  
- Улучшена стабильность работы JS-обработчика.  
- Оптимизирована очистка токенов во время валидации.  
- Улучшена работа в модальных окнах.  
- Улучшена совместимость с AjaxForm 3.x, FormIt и FetchIt.  
- Добавлена возможность отключать ассеты (`includeAssets=none`).  
- Оптимизирована генерация токенов и PNG-картинок.

---

## Требования

- **MODX Revolution** 2.6+ или 3.x  
- **PHP 7.2+** (рекомендуется 7.4+)  
- **GD + TrueType** (для рендеринга TTF-шрифта)

---

## Установка

1. Установите пакет через установщик MODX.  
2. Убедитесь, что существует файл:

assets/components/scxcaptchaajaxform/fonts/DejaVuSansMono.ttf

*(Если его нет — будет использован bitmap-фолбэк.)*

---

## Быстрый старт

Вставьте блок капчи в форму (**некэшируемо!**):

```html
[[!ScxCaptchaAjaxForm]]
Пример с AjaxForm
html
Копировать код
[[!AjaxForm?
  &snippet=`FormIt`
  &form=`tpl.AjaxForm.example`
  &hooks=`ScxCaptchaAjaxFormHook,email`
  &validate=`name:required,email:required:email,scx_code:required`
]]
```

### Пример с FormIt
```html
[[!FormIt?
  &hooks=`ScxCaptchaAjaxFormHook,email`
  &validate=`name:required,email:required:email,scx_code:required`
]]

[[!ScxCaptchaAjaxForm]]
```

### Пример с FetchIt
```html
[[!FetchIt?
   &prepare=`ScxCaptchaAjaxFormHook`
   &validate=`name:required,email:required:email,scx_code:required`
]]

[[!ScxCaptchaAjaxForm]]
```

### Параметры сниппета
Параметр ||	По умолчанию ||	Описание ||
ttl ||	1200 ||	Время жизни токена (сек). ||
includeAssets ||	head ||	head — CSS/JS в <head>, inline — рядом с HTML, none — не подключать. ||
render	|| 1	|| 1 — вернуть HTML блока, 0 — вернуть только ассеты. ||

### Примеры параметров
1. Ассеты в <head> (по умолчанию)
2. 
```html
[[!ScxCaptchaAjaxForm]]
```

2. Ассеты inline (например для модалок)
```html
[[!ScxCaptchaAjaxForm? &includeAssets=`inline`]]
```

3. Только ассеты, без HTML
```html
[[!ScxCaptchaAjaxForm? &render=`0`]]
```

4. Ассеты уже подключены вручную
```html
[[!ScxCaptchaAjaxForm? &includeAssets=`none`]]
```

### Поля формы
Поле ||	Назначение ||
scx_hp ||	honeypot (должно быть пустым) ||
scx_ts ||	timestamp рендера формы (мс), антибот ≥ 3 сек ||
scx_code ||	код пользователя (5 цифр) ||
scx_token ||	скрытый токен для текущей капчи ||

### Как это работает
Сниппет создаёт токен → кладёт в $_SESSION['scx_allowed'].
JS подставляет src для загрузки PNG-картинки.
captcha.php генерирует код (5 цифр), хэширует и сохраняет в $_SESSION['scx_code'][token].

### Хук ScxCaptchaAjaxFormHook валидирует:

-honeypot
-время
-токен
-код

### После успешной проверки данные очищаются.

## Отладка
## Добавьте &debug=1 к URL:

captcha.php?...&debug=1

Если видите “Bad token” — проверьте:
-капча вызывается некэшируемо: [[!ScxCaptchaAjaxForm]]

-страница и captcha.php на одном домене

-ассеты подключены корректно

-в форме AjaxForm есть поле <input name="af_action">

###Глобальное подключение ассетов
##Можно подключить CSS/JS один раз в шаблоне:

```html
<link rel="stylesheet" href="[[++assets_url]]components/scxcaptchaajaxform/scx.css">
<script src="[[++assets_url]]components/scxcaptchaajaxform/scx.js"></script>
```

## Кастомизация
### Переопределяйте классы:

```css
.scx-captcha,
.scx-img,
.scx-refresh,
.scx-input,
.scx-hp {}
```

## Безопасность
### Nокены короткоживущие

-код хранится в виде хэша

-защита honeypot + timestamp


Автор и поддержка
Разработчик: sait-modx.by
Email: info@sait-modx.by

Лицензия
MIT License
© 2025 — sait-modx.by
